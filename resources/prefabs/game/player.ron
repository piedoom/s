#![enable(implicit_some)]
Prefab (
    entities: [
        (
            data: (
                name: Named(name: "game::player"),
                player: Player(),
                transform: Transform(),
                controller: Controller(),
            ),
        ),
    ],
)
